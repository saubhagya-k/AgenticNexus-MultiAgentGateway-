syntax = "proto3";

service AIService {
  rpc Chat (ChatRequest) returns (ChatReply);
}

message ChatRequest {
  string message = 1;
  string thread_id = 2;
}

message ChatReply {
  string response = 1;
}